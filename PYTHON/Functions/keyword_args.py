# Keyword arguments, values are passed by explicitly specifying the parameter names
# so that order does not matter.

def stduent(fname, lname):
    print(fname)
    print(lname)
stduent(fname='a', lname='b')


