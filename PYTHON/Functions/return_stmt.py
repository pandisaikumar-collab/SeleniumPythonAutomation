# the return statement ends a function and
# sends a value back to the caller,
# it can return any data type, multiple values, (packet into a tuple),
# or None if no value is given

def m1(nums):
    return sum(nums)

nums = [10,20,30,40,50]
print(m1(nums))


